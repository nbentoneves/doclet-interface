package com.docletinterface.doclet.logic


class JsonCommandParser {

    fun parseObject(json: String, classRef: String): Any {
        //val classInit = Class.forName(classRef)
        //return ObjectMapper().readValue(json, classInit)
        return ""
    }

}